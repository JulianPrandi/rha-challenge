import{ makeStyles} from "@material-ui/core/styles"

export const useStyles = makeStyles({
    card:{
        alignContent:"center",
        paddingTop:"50px"
    },
    cardContent:{
        textAlign: "center"
    }
})